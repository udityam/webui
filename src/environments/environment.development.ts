export const environment = {
  production: false,
  // Absolute URL for local development — points directly to FastAPI dev server.
  // Override with a proxy (proxy.conf.json) or use this directly.
  apiBaseUrl: 'http://localhost:8000/api',
};
